{
  "id": "A12_patch_add_frame",
  "title": "Agent patches magic-number scatter into framed scatter",
  "kind": "patch",
  "handbooks": [],
  "seedSource": "tests/agent-exam/seeds/A12_magic_scatter.viva",
  "prompt": "Refactor this Viva artifact to use a top-level `frame plot` with xlim/ylim and put `frame: plot` on the mark nodes so x/y are data-domain values (remove magic scene coordinates on marks). Keep the same data field names. Output ONLY the full updated Viva source starting with artifact.\n\nCurrent source:\n{{source}}",
  "assertions": {
    "compiles": true,
    "mustMatch": ["(?m)^frame\\s+plot", "frame:\\s*plot"],
    "ir": {
      "minFrames": 1
    }
  }
}
