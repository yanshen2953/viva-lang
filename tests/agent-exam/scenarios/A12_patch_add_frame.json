{
  "id": "A12_patch_add_frame",
  "title": "Agent patches magic-number scatter into framed scatter",
  "kind": "patch",
  "handbooks": [],
  "seedSource": "tests/agent-exam/seeds/A12_magic_scatter.viva",
  "prompt": "Refactor this Viva artifact to use a TOP-LEVEL `frame plot`.\n\nCRITICAL SYNTAX:\n- Add `frame plot` as a sibling of `scene` (NOT indented under scene, NOT `frame: plot` as a block).\n- Put property `frame: plot` on mark nodes; x/y become data-domain values.\n- Frame body uses `x:`/`y:` scene ranges and `xlim:`/`ylim:` data domain.\n\nKeep the same data field names. Output ONLY the full updated Viva source starting with artifact.\n\nCurrent source:\n{{source}}",
  "assertions": {
    "compiles": true,
    "mustMatch": ["(?m)^frame\\s+plot", "frame:\\s*plot"],
    "forbidMatch": ["(?m)^[ \\t]+frame\\s+plot"],
    "ir": {
      "minFrames": 1
    }
  }
}
